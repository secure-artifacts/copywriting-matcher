# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# PyInstaller
# We do NOT ignore *.spec files because they are required for CI build
build/
build_folder/
dist/
dist_folder/
dist_final/
local_build/

# User configurations & Local Data
app_settings.json
custom_background.dat

# All Excel files — no user data/inventory shipped with the release
*.xlsx

# Test & verification scripts (local only, not for release)
test_temp/
verify_inventory.xlsx
verify_input.xlsx
verify_result.xlsx
verify_run.py
test_matcher.py

# Environments
.env
.env.*
.venv
venv/
ENV/
env/

# IDEs
.idea/
.vscode/
.gemini/
